# AI Sana Challenge Hub

**От бизнес-задачи к первому решению**

AI Sana Challenge Hub — MVP платформы, которая помогает бизнесу превратить короткое описание проблемы в понятную задачу для студенческих команд.

Главная механика проекта — **рейтинг готовности бизнес-задачи**.

Чем полнее и полезнее бизнес описывает задачу, тем выше её рейтинг и позиция в каталоге.

---

## Основной сценарий

```text
Бизнес
  ↓
Короткий черновик задачи
  ↓
AI-уточнение
  ↓
Редактируемая карточка
  ↓
Рейтинг готовности 0–100
  ↓
Подтверждение бизнеса
  ↓
Публикация в каталоге
  ↓
Отклик студенческой команды
  ↓
Ручное решение бизнеса
```

Система **не назначает команды автоматически**.

Студенты самостоятельно выбирают задачи, а бизнес самостоятельно принимает решение по полученным предложениям.

---

# Быстрый запуск

Требуется Python 3.

Клонировать репозиторий:

```bash
git clone https://github.com/BAITC-Hacks/hack-a39db617-dsa.git
cd hack-a39db617-dsa
```

Запустить приложение:

```bash
python3 server.py
```

Открыть:

```text
http://127.0.0.1:8765
```

Дополнительные зависимости для базового запуска не требуются.

---

# Что реализовано

## Для бизнеса

Бизнес может:

- создать черновик задачи;
- выбрать направление;
- получить уточняющие вопросы;
- дополнить информацию;
- отредактировать сформированную карточку;
- увидеть предварительный рейтинг;
- подтвердить сведения;
- опубликовать даже слабую задачу;
- просматривать свои задачи;
- получать отклики команд;
- вручную выбрать или отклонить предложение.

## Для студентов

Студент может:

- просматривать общий каталог;
- искать задачи;
- фильтровать их по направлению и уровню готовности;
- открыть полную карточку;
- выбрать свою команду при отправке отклика;
- отправить идею, план, срок и ссылку;
- просматривать свои отклики;
- видеть решение бизнеса.

---

# Рейтинг готовности

Рейтинг оценивает **полноту подтверждённого описания задачи**, а не качество компании или команды.

Максимум — **100 баллов**.

| Критерий | Баллы |
|---|---:|
| Контекст | 10 |
| Потребность бизнеса | 10 |
| Данные и материалы | 20 |
| Ожидаемый результат | 15 |
| Критерии успеха | 15 |
| Ограничения | 10 |
| Пользователи | 10 |
| Контакт | 5 |
| Формат взаимодействия | 5 |
| **Итого** | **100** |

## Уровни

| Баллы | Уровень |
|---:|---|
| 0–39 | Черновик |
| 40–69 | Рабочая |
| 70–89 | Готовая |
| 90–100 | Приоритетная |

Заполненные сведения учитываются в итоговом рейтинге только после подтверждения бизнесом.

Если карточка изменяется, сведения необходимо подтвердить повторно.

---

# Влияние рейтинга на каталог

Рейтинг используется как основная геймификационная механика.

```text
Более полная задача
        ↓
Более высокий рейтинг
        ↓
Более высокая позиция в каталоге
```

Низкий рейтинг **не блокирует публикацию**.

Даже задача уровня «Черновик» может быть опубликована и оставаться доступной студенческим командам.

При одинаковом рейтинге новые задачи могут отображаться выше.

---

# AI-функция

В MVP реализован локальный AI-режим.

Он используется для уточнения исходного описания задачи.

Система:

1. получает черновик, направление и уже известные сведения;
2. определяет недостающие поля;
3. формирует релевантные уточняющие вопросы;
4. возвращает структурированный результат;
5. не добавляет неизвестные факты самостоятельно.

Пользователь может:

- изменить ответы;
- оставить неизвестные сведения пустыми;
- отредактировать итоговую карточку;
- самостоятельно подтвердить сведения перед публикацией.

## Ограничение AI

AI не может:

- придумывать отсутствующие факты;
- назначать студенческую команду;
- скрывать задачи от студентов;
- принимать решение вместо бизнеса.

Контракт AI описан отдельно:

```text
docs/AI-CONTRACT.md
```

Внешнюю LLM можно подключить через тот же серверный контракт без изменения основного пользовательского сценария.

---

# Карточка задачи

Карточка содержит:

- название;
- контекст;
- потребность бизнеса;
- пользователей;
- данные и материалы;
- ограничения;
- ожидаемый результат;
- критерии успеха;
- контакт;
- формат взаимодействия.

Все поля можно отредактировать перед подтверждением.

---

# Отклик команды

Для отправки предложения студент указывает:

- команду;
- идею решения;
- план работы;
- срок;
- ссылку на прототип или результат.

После отправки предложение получает статус:

```text
На рассмотрении
```

Бизнес затем вручную может выбрать:

```text
Выбрана
```

или:

```text
Отклонена
```

Автоматического назначения исполнителя нет.

---

# Сквозная проверка

Для быстрой проверки проекта:

### 1. Бизнес

Выбрать роль:

```text
Бизнес
```

Нажать:

```text
Создать задачу
```

Можно использовать кнопку:

```text
Подставить демо-пример
```

После отправки черновика система предложит уточняющие вопросы.

---

### 2. Карточка и рейтинг

Ответить на вопросы или оставить часть сведений пустыми.

Перейти к карточке.

Справа отображается предварительный рейтинг.

Заполнение недостающих сведений увеличивает рейтинг.

Перед публикацией необходимо подтвердить карточку.

---

### 3. Публикация

Нажать:

```text
Подтвердить и опубликовать
```

После этого задача появляется в каталоге.

---

### 4. Студент

Переключить роль:

```text
Студент
```

Открыть опубликованную задачу.

Выбрать команду и заполнить отклик.

Пример:

**Идея**

> Сравним простой прогноз со средним за неделю и покажем рекомендуемый объём закупок по категориям.

**План**

```text
Неделя 1: проверка CSV.
Неделя 2: базовый прогноз и оценка ошибки.
Неделя 3: панель и демонстрация.
```

**Срок**

```text
3 недели
```

**Ссылка**

```text
https://example.com/nomad-prototype
```

После отправки предложение появляется в разделе:

```text
Мои отклики
```

---

### 5. Решение бизнеса

Вернуться к роли:

```text
Бизнес
```

Открыть задачу и просмотреть отклики.

Бизнес самостоятельно выбирает:

```text
Выбрать команду
```

или:

```text
Отклонить
```

На этом основной сквозной сценарий MVP завершён.

Подробная инструкция:

```text
docs/DEMO.md
```

---

# Демонстрационные данные

В проекте используются синтетические данные.

Исходное состояние содержит:

- демонстрационные задачи разной степени готовности;
- профили студенческих команд;
- примеры предложений.

Это позволяет проверить каталог, рейтинг и отклики сразу после запуска.

---

# Архитектура

```text
hack-a39db617-dsa/
│
├── server.py
│
├── README.md
│
├── static/
│   ├── index.html
│   ├── app.js
│   └── style.css
│
├── tests/
│   └── test_hub.py
│
└── docs/
    ├── AI-CONTRACT.md
    ├── DEMO.md
    └── TEAM-PLAN.md
```

## Backend

```text
server.py
```

Отвечает за:

- HTTP API;
- хранение данных;
- рейтинг;
- AI-уточнение;
- задачи;
- отклики;
- решения бизнеса.

## Frontend

```text
static/index.html
static/app.js
static/style.css
```

Интерфейс работает без отдельного frontend-framework.

## Хранение

Данные сохраняются локально в SQLite.

---

# Основные API

```text
GET  /api/state
POST /api/ai
POST /api/tasks
POST /api/proposals
POST /api/decision
```

`/api/state` возвращает состояние, необходимое клиентскому приложению.

`/api/ai` выполняет локальное уточнение задачи.

`/api/tasks` создаёт и обновляет карточки.

`/api/proposals` сохраняет отклики студенческих команд.

`/api/decision` сохраняет ручное решение бизнеса.

---

# Проверка тестов

Запуск:

```bash
python3 -m unittest discover -s tests
```

или:

```bash
python3 -m unittest tests/test_hub.py
```

---

# Ограничения MVP

Проект разработан как MVP в рамках пятичасового хакатона.

Намеренно не реализованы:

- полноценная регистрация;
- восстановление пароля;
- сложная ролевая модель;
- чат в реальном времени;
- уведомления;
- календарь;
- файловое хранилище;
- обучение собственной ML-модели;
- векторная база данных;
- адаптация под мобильные устройства;
- production-инфраструктура;
- полноценный project tracker.

Эти функции не нужны для проверки основного сценария кейса.

---

# Основной принцип проекта

> Чем полнее и полезнее бизнес описывает задачу, тем выше её рейтинг и позиция в каталоге.

AI помогает уточнить задачу, но финальные сведения подтверждает человек.

Студенты самостоятельно выбирают задачи.

Бизнес самостоятельно выбирает команды.

---

# Документация

Дополнительные материалы:

```text
docs/AI-CONTRACT.md
```

Контракт AI, формат входных и выходных данных и правила обработки.

```text
docs/DEMO.md
```

Пошаговая инструкция для проверки работающего MVP.

```text
docs/TEAM-PLAN.md
```

Структура командной работы над проектом.

---

## AI Sana Challenge Hub

**От бизнес-задачи к первому решению.**


## Исходные данные по разделу 6 ТЗ

Набор находится в [fixtures/demo-data.json](fixtures/demo-data.json). Он содержит четыре связанные коллекции по пять записей:

| Коллекция | Обязательные сведения |
|---|---|
| `drafts` | `text` — исходное описание разной полноты, `industry` — отрасль, `task_id` — связь с карточкой |
| `tasks` | `fields` со всеми полями рейтинга, `score`, `level`, `breakdown`, `missing`, направление и подтверждение |
| `teams` | Название, интересы, навыки, технологии |
| `proposals` | Ссылки на команду и задачу, идея, план, срок (`duration`), ссылка (`link`) |

Рейтинги пяти карточек: **100, 85, 65, 30 и 10**. Два разных предложения для кофейни позволяют показать сравнение команд бизнесом. Остальные отклики относятся к образованию, экологии и логистике. Контакты и ссылки синтетические, внешние прототипы по адресам example.com не предоставляются.

JSON можно скачать на сайте в разделе **«Как это работает» → «Скачать JSON-набор»**. API `/api/demo-data` возвращает исходный учебный набор, а `/api/state` — актуальную рабочую базу с пользовательскими изменениями.

При первом запуске новой базы приложение читает этот файл. Существующая SQLite-база остаётся без изменений. Для просмотра нового набора без удаления текущей работы:

```bash
SANA_DB=data/new-demo.sqlite3 python3 server.py --port 8768
```

PowerShell:

```powershell
$env:SANA_DB = "data/new-demo.sqlite3"
py server.py --port 8768
```

### Удобство и достоверность демонстрации

Незавершённый конструктор автоматически сохраняет последнюю редактируемую карточку в этом браузере. После перезагрузки в каталоге появляется «Продолжить заполнение». Можно вернуться на предыдущий шаг, не теряя ответы. Локальная копия не публикуется и не заменяет сохранение на сервере. Восстановление всегда требует нового подтверждения, при конфликте серверной версии нужно заново открыть актуальную карточку. Локально хранится одна последняя редактируемая карточка; открытие другой карточки для редактирования заменяет её. После успешного сохранения на сервере локальная копия удаляется.

XP команды начинается с **0** и складывается только из баллов подтверждённых этапов. Кабинет и список команд показывают одинаковый результат. Порог уровней: 0, 50, 100 и 200 XP; последний уровень максимальный.

Проверки: `python3 -m unittest discover -s tests -v` (12 тестов, включая объём, связи и рейтинг набора). Для дополнительных регрессионных проверок интерфейсной логики, если установлен Node.js: `node --test tests/test_ui.js` (6 тестов). Node.js не требуется для запуска сайта.
